version: '3'
services:
  jupyter:
    image: jupyter/datascience-notebook
    ports:
     - "8888:8888"
    volumes:
      - .:/home/jovyan/work
    environment:
      - NB_UID=${NB_UID}
      - NB_GID=${NB_GID}
      - GRANT_SUDO=yes
